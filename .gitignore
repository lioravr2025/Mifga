node_modules
dist
dist-ssr
*.local
.DS_Store
*.tsbuildinfo

.vercel

# Firebase credentials - google-services.json is safe to commit (it's a
# public client identifier, not a secret), but any service-account /
# admin-sdk key must never enter git history.
*firebase-adminsdk*.json
*serviceAccountKey*.json
supabase/.temp
supabase/functions/**/.env
